version: 2
models:
  - name: dim_orders
    description: A dim table containing all information about a particular order
    columns:
      - name: order_id
        description: '{{ doc("order_id") }}' 
      - name: customer_id
      - name: order_date
      - name: bank_transfer_amount
      - name: total_amount
