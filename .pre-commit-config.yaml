default_stages: [commit]

repos:
  - repo: local
    rev: v3.4.0
    hooks:
      - id: check-merge-conflict
      - id: check-yaml
        exclude: ^docs|/target/
      - id: end-of-file-fixer
        exclude: ^docs|/target/
      - id: trailing-whitespace
        exclude: ^docs|/target/